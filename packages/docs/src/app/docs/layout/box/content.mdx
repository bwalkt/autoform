import { Box, Flex, Heading, Text } from '@bwalkt/ui'
import { CodeBlock } from '../../../../components/CodeBlock'
import { LiveCodeBlock } from '../../../../components/LiveCodeBlock'
import { ThemesPropsTable } from '../../../../components/ThemesPropsTable'
import { ViewportPreview } from '../../../../components/ViewportPreview'

<Heading as="h1" size="6">
  Box
</Heading>
<Text size="2" className="mt-2 text-muted-foreground">
  Fundamental layout building block. Use it to apply spacing, display, and sizing props without leaving the component
  API.
</Text>

<LiveCodeBlock
  initialCode={`export default function Example() {
  return (
    <Box p="4" className="rounded-2xl border border-border/70 bg-muted/30">
      <Box className="rounded-xl border border-border/70 bg-background px-4 py-3 text-sm text-muted-foreground">
        Box content
      </Box>
    </Box>
  )
}`}
/>

<Heading as="h2" size="4" id="api-reference" data-heading>
  API Reference
</Heading>

This component is based on the `div` element and supports common margin props.

<ThemesPropsTable defs="layoutPropDefs" />

<Heading as="h2" size="4" id="examples" data-heading>
  Examples
</Heading>

<Heading as="h3" size="3" id="responsive" data-heading>
  Responsive
</Heading>

<Box className="mt-2 text-sm text-muted-foreground">
  Responsive props accept breakpoint objects. For example, this box increases padding from `3` to `5` at the medium
  breakpoint.
</Box>

<CodeBlock.Root mt="6">
  <CodeBlock.LivePreview>
    <ViewportPreview>
      <Box p={{ initial: '3', md: '5' }} className="rounded-2xl border border-border/70 bg-muted/30">
        <Box className="rounded-xl border border-border/70 bg-background px-4 py-3 text-sm text-muted-foreground">
          Box content
        </Box>
      </Box>
    </ViewportPreview>
  </CodeBlock.LivePreview>
  <CodeBlock.Content>
    <CodeBlock.Pre>
      <CodeBlock.Code language="jsx">
{`<Box p={{ initial: "3", md: "5" }} className="rounded-2xl border border-border/70 bg-muted/30">
  <Box className="rounded-xl border border-border/70 bg-background px-4 py-3 text-sm text-muted-foreground">
    Box content
  </Box>
</Box>`}
      </CodeBlock.Code>
    </CodeBlock.Pre>
  </CodeBlock.Content>
</CodeBlock.Root>
