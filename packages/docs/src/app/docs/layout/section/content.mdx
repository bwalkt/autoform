import { Box, Heading, Section, Text } from '@bwalkt/ui'
import { CodeBlock } from '../../../../components/CodeBlock'
import { DecorativeBox } from '../../../../components/DecorativeBox'
import { LiveCodeBlock } from '../../../../components/LiveCodeBlock'
import { ThemesPropsTable } from '../../../../components/ThemesPropsTable'
import { ViewportPreview } from '../../../../components/ViewportPreview'

<Heading as="h1" size="6">
  Section
</Heading>
<Text size="2" className="mt-2 text-muted-foreground">
  Vertical spacing wrapper for page sections. Use it to keep stacked content consistent.
</Text>

<LiveCodeBlock
  initialCode={`export default function Example() {
  return (
    <Section size="2" className="rounded-2xl border border-border/70 bg-muted/30">
      <DecorativeBox style={{ height: 64 }} />
    </Section>
  )
}`}
/>

<Heading as="h2" size="4" id="api-reference" data-heading>
  API Reference
</Heading>

This component is based on the `div` element and supports common margin props.

<ThemesPropsTable defs="layoutPropDefs" />

<Heading as="h2" size="4" id="examples" data-heading>
  Examples
</Heading>

<Heading as="h3" size="3" id="responsive" data-heading>
  Responsive
</Heading>

<Box className="mt-2 text-sm text-muted-foreground">
  Responsive props accept breakpoint objects. For example, this section increases its spacing at the medium breakpoint.
</Box>

<CodeBlock.Root style={{ marginTop: 24 }}>
  <CodeBlock.LivePreview>
    <ViewportPreview>
      <Section size={{ initial: '1', md: '3' }} className="rounded-2xl border border-border/70 bg-muted/30">
        <DecorativeBox />
      </Section>
    </ViewportPreview>
  </CodeBlock.LivePreview>
  <CodeBlock.Content>
    <CodeBlock.Pre>
      <CodeBlock.Code language="jsx">
{`<Section size={{ initial: "1", md: "3" }} className="rounded-2xl border border-border/70 bg-muted/30">
  <DecorativeBox />
</Section>`}
      </CodeBlock.Code>
    </CodeBlock.Pre>
  </CodeBlock.Content>
</CodeBlock.Root>
