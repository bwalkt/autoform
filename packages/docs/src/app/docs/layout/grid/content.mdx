import { Box, Flex, Grid, Heading, Text } from '@bwalkt/ui'
import { CodeBlock } from '../../../../components/CodeBlock'
import { DecorativeBox } from '../../../../components/DecorativeBox'
import { LiveCodeBlock } from '../../../../components/LiveCodeBlock'
import { ThemesPropsTable } from '../../../../components/ThemesPropsTable'
import { ViewportPreview } from '../../../../components/ViewportPreview'

<Heading as="h1" size="6">
  Grid
</Heading>
<Text size="2" className="mt-2 text-muted-foreground">
  Component for creating grid layouts.
</Text>
<LiveCodeBlock 
  initialCode={`export default function Example() {
  // Try changing columns to "4" or "2"  
  // Also try using inline style as a test: style={{ gridTemplateColumns: 'repeat(4, 1fr)' }}
  return (
    <Grid columns="3" gap="3" width="auto">
      <DecorativeBox style={{ height: 64 }} />
      <DecorativeBox style={{ height: 64 }} />
      <DecorativeBox style={{ height: 64 }} />
      <DecorativeBox style={{ height: 64 }} />
      <DecorativeBox style={{ height: 64 }} />
      <DecorativeBox style={{ height: 64 }} />
      <DecorativeBox style={{ height: 64 }} />
      <DecorativeBox style={{ height: 64 }} />
    </Grid>
  )
}`}
/>

<Heading as="h2" size="4" id="api-reference" data-heading>
  API Reference
</Heading>

This component is based on the `div` element and supports common margin props.

Use these props to create grid layouts.

<ThemesPropsTable defs="gridPropDefs" />

The following props are shared between Box, Flex, Grid, Container and Section components.

<ThemesPropsTable defs="layoutPropDefs" />

<Heading as="h2" size="4" id="examples" data-heading>
  Examples
</Heading>

<Heading as="h3" size="3" id="responsive" data-heading>
  Responsive
</Heading>

All props marked `Responsive`, such as `columns` and `rows` accept a breakpoint object. For example, the following grid starts with 1 column, and uses 2 columns from the medium breakpoint.

<CodeBlock.Root style={{ marginTop: 24 }}>
  <CodeBlock.LivePreview>
    <ViewportPreview>
      <Grid columns={{ initial: '1', md: '2' }} gap="3" width="auto">
        <Box height="64px">
          <DecorativeBox />
        </Box>
        <Box height="64px">
          <DecorativeBox />
        </Box>
      </Grid>
    </ViewportPreview>
  </CodeBlock.LivePreview>
  <CodeBlock.Content>
    <CodeBlock.Pre>
      <CodeBlock.Code language="jsx">
{`<Grid columns={{ initial: "1", md: "2" }} gap="3" width="auto">
  <Box height="64px">
    <DecorativeBox />
  </Box>
  <Box height="64px">
    <DecorativeBox />
  </Box>
</Grid>`}
      </CodeBlock.Code>
    </CodeBlock.Pre>
  </CodeBlock.Content>
</CodeBlock.Root>